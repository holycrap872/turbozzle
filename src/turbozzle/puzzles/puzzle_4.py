#!/usr/bin/env python3
from turbozzle.utils.movement_wrapper import forward, init_puzzle, left, wait_until_exit


def helper_function_1():
    for i in range(7):
        "CAN REPLACE"


def solve_puzzle_4():
    for i in range("MUST REPLACE"):
        "CAN REPLACE"
        "CAN REPLACE"
        "CAN REPLACE"
        "CAN REPLACE"
        "CAN REPLACE"
        "CAN REPLACE"
        "CAN REPLACE"


init_puzzle("levels/puzzle_4.png", x="MUST REPLACE", y="MUST REPLACE", speed=1)
solve_puzzle_4()

wait_until_exit()
